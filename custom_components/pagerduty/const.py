"""Constants for the PagerDuty integration."""

# Based on where this is executed I'd probably move these to the __init__ file 
# That is more "pythonic than having a source with only two variables."

DOMAIN = "pagerduty"
CONF_API_KEY = "api_key"
